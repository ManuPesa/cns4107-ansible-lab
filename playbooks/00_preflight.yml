---
- name: Preflight check
  hosts: iosxe
  gather_facts: false

  tasks:
    - name: Run show version
      cisco.ios.ios_command:
        commands:
          - show version
      register: result

    - debug:
        var: result.stdout_lines
